# SPEC 01 — Foundation

**Phase:** 0
**Goal:** Establish project infrastructure, tooling, and architectural skeleton.

---

## Files to CREATE

### Frontend — Routing & Layout

#### `src/routes/__root.tsx`
- Create the root layout component for TanStack Router
- Implements the main application shell: sidebar + main content area + status bar
- Wraps children with `<Outlet />`
- Includes `<Toaster />` from sonner for toast notifications
- Includes the `<CommandPalette />` component (Cmd+K)

#### `src/routes/index.tsx`
- Create the dashboard/home route (`/`)
- Displays a welcome screen or quick-access tiles (recent services, recently played hymns)
- Placeholder content for Phase 0

#### `src/routeTree.gen.ts`
- Auto-generated by TanStack Router CLI
- Contains the typed route tree for the application

### Frontend — Stores (Zustand)

#### `src/stores/ui-store.ts`
- Create Zustand store for UI state
- State: `sidebarOpen: boolean`, `sidebarWidth: number`, `activePanel: string | null`, `modals: Record<string, boolean>`
- Actions: `toggleSidebar()`, `setSidebarWidth(w)`, `openModal(id)`, `closeModal(id)`, `setActivePanel(id)`

#### `src/stores/theme-store.ts`
- Create Zustand store for theme management
- State: `theme: 'azure' | 'white' | 'gray' | 'orange' | 'black'`, `language: 'pt' | 'es' | 'en'`
- Actions: `setTheme(theme)`, `setLanguage(lang)`
- Persists to localStorage on change
- On init, reads from localStorage and applies `data-theme` attribute to `<html>`

#### `src/stores/presentation-store.ts`
- Create Zustand store skeleton for presentation state
- State: `currentPresentationId: number | null`, `activeSlideIndex: number`, `isProjectorOpen: boolean`
- Actions: placeholder stubs for Phase 0

#### `src/stores/audio-store.ts`
- Create Zustand store skeleton for audio playback state
- State: `status: 'idle' | 'playing' | 'paused'`, `currentFile: string | null`, `positionMs: number`, `durationMs: number`, `volume: number`
- Actions: placeholder stubs for Phase 0

#### `src/stores/display-store.ts`
- Create Zustand store skeleton for monitor/display management
- State: `monitors: MonitorInfo[]`, `monitorAssignments: Record<string, string>`, `projectorWindowOpen: boolean`, `returnWindowOpen: boolean`
- Actions: placeholder stubs for Phase 0

### Frontend — Lib / Utilities

#### `src/lib/utils.ts`
- Create the `cn()` utility function using `clsx` + `tailwind-merge`
- Export helper for merging Tailwind classes

#### `src/lib/tauri.ts`
- Create typed wrapper functions for all Tauri `invoke()` calls
- Phase 0: only `greet` command wrapper
- Export a generic `tauriInvoke<T>(command, args)` helper with error handling

#### `src/lib/queries.ts`
- Create TanStack Query key factories and query/mutation function skeletons
- Phase 0: define the pattern with `queryKeys` object and one example query factory

#### `src/lib/constants.ts`
- Create application constants
- `ASPECT_RATIOS`: `['free', '4:3', '16:9']`
- `THEMES`: `['azure', 'white', 'gray', 'orange', 'black']`
- `LANGUAGES`: `['pt', 'es', 'en']`
- `DEFAULT_THEME`: `'azure'`
- `DEFAULT_LANGUAGE`: `'pt'`

#### `src/lib/i18n.ts`
- Create i18next configuration
- Initialize i18next with `react-i18next`
- Load translation JSON files from `src/locales/`
- Default language: `pt`
- Fallback language: `en`

### Frontend — Internationalization

#### `src/locales/pt.json`
- Create Portuguese translation file
- Include keys for: app name, sidebar navigation labels, common actions (save, cancel, delete, search), theme names

#### `src/locales/en.json`
- Create English translation file
- Same key structure as `pt.json`

#### `src/locales/es.json`
- Create Spanish translation file
- Same key structure as `pt.json`

### Frontend — Types

#### `src/types/hymn.ts`
- Create TypeScript types: `Hymn`, `Album`, `HymnSearchResult`
- Fields match the `hymns` SQLite table schema

#### `src/types/bible.ts`
- Create TypeScript types: `BibleVersion`, `Book`, `Chapter`, `Verse`, `BibleSearchResult`
- Fields match the `bible_versions` and `bible_verses` SQLite tables

#### `src/types/presentation.ts`
- Create TypeScript types: `Presentation`, `Slide`, `SlideContent`, `SlideType`
- `SlideContent` is a discriminated union by `type`: `'cover' | 'lyrics' | 'pause' | 'text' | 'image' | 'bible' | 'video'`

#### `src/types/service.ts`
- Create TypeScript types: `Service`, `ServiceItem`, `ServiceItemType`
- `ServiceItemType`: `'hymn' | 'bible' | 'presentation' | 'annotation' | 'url' | 'file'`

#### `src/types/settings.ts`
- Create TypeScript types: `Settings`, `MonitorConfig`, `MonitorRole`
- `MonitorRole`: `'operator' | 'projector' | 'return'`

#### `src/types/audio.ts`
- Create TypeScript types: `AudioState`, `PlaybackStatus`
- `PlaybackStatus`: `'idle' | 'playing' | 'paused' | 'seeking'`

### Frontend — Layout Components

#### `src/components/layout/sidebar.tsx`
- Create the main navigation sidebar component
- Navigation items: Home, Hymnal, Bible, Presentations, Services, Utilities, Settings
- Uses `lucide-react` icons for each item
- Collapsible with toggle button
- Highlights active route using TanStack Router's `useMatch`
- Uses i18next for labels

#### `src/components/layout/header.tsx`
- Create the top bar component
- Contains: search input (opens command palette on focus), clock display, settings gear icon
- Uses i18next for labels

#### `src/components/layout/status-bar.tsx`
- Create the bottom status bar component
- Displays: connection status, current projector state, streaming status (placeholder)
- Minimal design, thin bar at the bottom

### Frontend — UI Primitives (Radix-based)

#### `src/components/ui/button.tsx`
- Rewrite the existing `src/button.tsx` as a proper component
- Use `class-variance-authority` or manual variants with Tailwind
- Variants: `default`, `outline`, `ghost`, `destructive`
- Sizes: `sm`, `md`, `lg`
- Uses `cn()` for class merging

#### `src/components/ui/dialog.tsx`
- Create modal dialog component wrapping `@radix-ui/react-dialog`
- Exports: `Dialog`, `DialogTrigger`, `DialogContent`, `DialogHeader`, `DialogTitle`, `DialogDescription`, `DialogFooter`
- Styled with Tailwind + backdrop blur

#### `src/components/ui/input.tsx`
- Create input component with Tailwind styling
- Supports: `label`, `error`, `disabled` states
- Forwarded ref

#### `src/components/ui/select.tsx`
- Create select dropdown wrapping `@radix-ui/react-select`
- Exports: `Select`, `SelectTrigger`, `SelectContent`, `SelectItem`, `SelectValue`

#### `src/components/ui/tabs.tsx`
- Create tabs component wrapping `@radix-ui/react-tabs`
- Exports: `Tabs`, `TabsList`, `TabsTrigger`, `TabsContent`

#### `src/components/ui/dropdown-menu.tsx`
- Create dropdown menu wrapping `@radix-ui/react-dropdown-menu`
- Exports: `DropdownMenu`, `DropdownMenuTrigger`, `DropdownMenuContent`, `DropdownMenuItem`, `DropdownMenuSeparator`

#### `src/components/ui/tooltip.tsx`
- Create tooltip component wrapping `@radix-ui/react-tooltip`
- Exports: `Tooltip`, `TooltipTrigger`, `TooltipContent`, `TooltipProvider`

#### `src/components/ui/scroll-area.tsx`
- Create scroll area wrapping `@radix-ui/react-scroll-area`
- Exports: `ScrollArea`, `ScrollBar`

#### `src/components/ui/slider.tsx`
- Create slider component wrapping `@radix-ui/react-slider`
- Styled for audio progress bars and volume controls

#### `src/components/ui/badge.tsx`
- Create badge component for labels/tags
- Variants: `default`, `secondary`, `outline`, `destructive`

#### `src/components/ui/card.tsx`
- Create card component
- Exports: `Card`, `CardHeader`, `CardTitle`, `CardDescription`, `CardContent`, `CardFooter`

#### `src/components/ui/command-palette.tsx`
- Create command palette wrapping `cmdk`
- Opens with Cmd+K (or Ctrl+K on Windows/Linux)
- Searchable list of all app actions/routes
- Keyboard navigable

### Frontend — Styles

#### `src/global.css`
- **UPDATE** existing file
- Add Tailwind v4 CSS-first config imports
- Add CSS custom properties for all 5 theme variants (azure, white, gray, orange, black)
- Add `[data-theme="..."]` selectors for each theme
- Define base font, scrollbar styles, selection styles

### Backend — Rust Module Structure

#### `src-tauri/src/commands/mod.rs`
- Create module re-exports file
- `pub mod music;`
- `pub mod bible;`
- `pub mod slides;`
- `pub mod liturgy;`
- `pub mod audio;`
- `pub mod display;`
- `pub mod streaming;`
- `pub mod settings;`
- `pub mod timer;`
- `pub mod utility;`

#### `src-tauri/src/commands/music.rs`
- Create empty command handler stubs for music/lyrics
- Stub commands: `search_hymns`, `get_hymn`, `get_albums`, `get_hymns_by_album`, `create_hymn`, `update_hymn`, `delete_hymn`
- Each returns `Result<T, AppError>` with a `todo!()` body

#### `src-tauri/src/commands/bible.rs`
- Create empty command handler stubs for Bible
- Stub commands: `get_bible_versions`, `get_books`, `get_verses`, `search_bible`

#### `src-tauri/src/commands/slides.rs`
- Create empty command handler stubs for slides/presentations
- Stub commands: `get_presentations`, `get_presentation`, `create_presentation`, `update_presentation`, `delete_presentation`, `get_slides`, `create_slide`, `update_slide`, `delete_slide`, `reorder_slides`

#### `src-tauri/src/commands/liturgy.rs`
- Create empty command handler stubs for worship services
- Stub commands: `get_services`, `get_service`, `create_service`, `update_service`, `delete_service`, `add_service_item`, `remove_service_item`, `reorder_service_items`

#### `src-tauri/src/commands/audio.rs`
- Create empty command handler stubs for audio
- Stub commands: `audio_play`, `audio_pause`, `audio_stop`, `audio_seek`, `audio_set_volume`, `audio_get_position`, `audio_get_status`

#### `src-tauri/src/commands/display.rs`
- Create empty command handler stubs for display/monitor management
- Stub commands: `get_available_monitors`, `open_projector_window`, `close_projector_window`, `open_return_window`, `close_return_window`, `set_monitor_config`

#### `src-tauri/src/commands/streaming.rs`
- Create empty command handler stubs for HTTP streaming
- Stub commands: `start_streaming_server`, `stop_streaming_server`, `get_streaming_status`

#### `src-tauri/src/commands/settings.rs`
- Create empty command handler stubs for settings
- Stub commands: `get_setting`, `set_setting`, `get_all_settings`

#### `src-tauri/src/commands/timer.rs`
- Create empty command handler stubs for timer
- Stub commands: `start_timer`, `stop_timer`, `reset_timer`, `get_timer_state`

#### `src-tauri/src/commands/utility.rs`
- Create empty command handler stubs for utilities
- Stub commands: `run_lottery`, `format_text`

### Backend — Database

#### `src-tauri/src/db/mod.rs`
- Create database module
- Initialize SQLite connection using `rusqlite`
- Provide `init_db(app_data_dir) -> Result<Connection>` function
- Run migrations on startup
- Return connection wrapped in `Mutex` for thread safety

#### `src-tauri/src/db/migrations.rs`
- Create schema migration runner
- Migration v1: Create all tables from PRD Section 5 (hymns, bible_versions, bible_verses, presentations, slides, services, service_items, favorites, settings, monitor_configs)
- Create indexes (idx_hymns_number, idx_hymns_title, idx_bible_verses_lookup, idx_slides_presentation, idx_service_items_service, idx_favorites_type)
- Create FTS5 virtual tables (hymns_fts, bible_fts)

#### `src-tauri/src/db/models.rs`
- Create Rust struct definitions with `serde::Serialize` and `serde::Deserialize`
- Structs: `Hymn`, `Album`, `BibleVersion`, `Book`, `Verse`, `Presentation`, `Slide`, `Service`, `ServiceItem`, `Favorite`, `Setting`, `MonitorConfig`, `MonitorInfo`

#### `src-tauri/src/db/queries/mod.rs`
- Create module re-exports for query submodules
- `pub mod music;`
- `pub mod bible;`
- `pub mod liturgy;`
- `pub mod settings;`

#### `src-tauri/src/db/queries/music.rs`
- Create empty query function stubs for music
- Functions: `search_hymns`, `get_hymn_by_id`, `get_albums`, `get_hymns_by_album`, `insert_hymn`, `update_hymn`, `delete_hymn`

#### `src-tauri/src/db/queries/bible.rs`
- Create empty query function stubs for Bible
- Functions: `get_versions`, `get_books`, `get_verses`, `search_bible_text`

#### `src-tauri/src/db/queries/liturgy.rs`
- Create empty query function stubs for liturgy
- Functions: `get_services`, `get_service_by_id`, `insert_service`, `update_service`, `delete_service`, `get_service_items`, `insert_service_item`, `delete_service_item`, `reorder_items`

#### `src-tauri/src/db/queries/settings.rs`
- Create empty query function stubs for settings
- Functions: `get_setting`, `set_setting`, `get_all_settings`, `get_monitor_configs`, `set_monitor_config`

### Backend — Audio (skeleton)

#### `src-tauri/src/audio/mod.rs`
- Create module declaration
- `pub mod player;`
- `pub mod sync;`

#### `src-tauri/src/audio/player.rs`
- Create empty `AudioPlayer` struct skeleton
- Fields: placeholder comments for `sink`, `stream`, `stream_handle`, `current_file`, `duration_ms`
- Methods: `new()` constructor only (implementations in Phase 2)

#### `src-tauri/src/audio/sync.rs`
- Create empty audio-slide sync module
- Define `SyncPoint` struct: `{ slide_index: usize, timestamp_ms: u64 }`
- Placeholder for sync logic

### Backend — Archive (skeleton)

#### `src-tauri/src/archive/mod.rs`
- Create module declaration
- `pub mod manifest;`

#### `src-tauri/src/archive/manifest.rs`
- Create `Manifest` struct: `{ title: String, author: Option<String>, aspect_ratio: String, slide_count: usize }`
- Placeholder for parse/write methods

### Backend — Display (skeleton)

#### `src-tauri/src/display/mod.rs`
- Create module with placeholder for monitor detection and window lifecycle functions

### Backend — Streaming (skeleton)

#### `src-tauri/src/streaming/mod.rs`
- Create module with placeholder for HTTP streaming server logic

### Backend — State & Error

#### `src-tauri/src/state.rs`
- Create `AppState` struct holding `db: Mutex<Connection>`
- Create `AudioState` struct (placeholder for Phase 2)
- Both implement `Send + Sync`

#### `src-tauri/src/error.rs`
- Create unified `AppError` enum using `thiserror`
- Variants: `Database(#[from] rusqlite::Error)`, `Io(#[from] std::io::Error)`, `SerdeJson(#[from] serde_json::Error)`, `NotFound(String)`, `Internal(String)`
- Implement `serde::Serialize` for Tauri IPC serialization
- Implement `From<AppError> for tauri::InvokeError` (or `impl Into<InvokeError>`)

---

## Files to UPDATE

#### `src-tauri/src/lib.rs`
- Add `mod commands;`, `mod db;`, `mod audio;`, `mod archive;`, `mod display;`, `mod streaming;`, `mod state;`, `mod error;`
- In the builder: register all command stubs from `commands::*`
- In the `setup()` hook: initialize SQLite database via `db::init_db()`, store `AppState` in Tauri managed state
- Register `tauri_plugin_log` plugin

#### `src-tauri/Cargo.toml`
- Add Phase 0 Rust dependencies:
  - `rusqlite = { version = "0.31", features = ["bundled"] }`
  - `thiserror = "1.0"`
  - `anyhow = "1.0"`
  - `tokio = { version = "1", features = ["full"] }`
  - `chrono = { version = "0.4", features = ["serde"] }`
  - `uuid = { version = "1.0", features = ["v4", "serde"] }`

#### `src-tauri/tauri.conf.json`
- Update `windows` to include `"projector"` and `"return"` window definitions (hidden by default)
- Update capabilities to include all permissions listed in PRD Section 7

#### `src-tauri/capabilities/default.json`
- Update permissions array to include all required Tauri 2 permissions (core:window:allow-create, core:window:allow-close, core:window:allow-set-position, etc.)

#### `package.json`
- Add Phase 0 frontend dependencies:
  - `@tanstack/react-router`, `@tanstack/react-query`, `zustand`
  - `@radix-ui/react-dialog`, `@radix-ui/react-dropdown-menu`, `@radix-ui/react-tabs`, `@radix-ui/react-scroll-area`, `@radix-ui/react-slider`, `@radix-ui/react-tooltip`, `@radix-ui/react-select`, `@radix-ui/react-toggle-group`
  - `lucide-react`, `cmdk`, `sonner`
  - `i18next`, `react-i18next`
- Add dev dependencies: `@tanstack/router-plugin` (for Vite route generation)

#### `vite.config.ts`
- Add TanStack Router Vite plugin for automatic route tree generation

#### `src/main.tsx`
- Replace the current React render with TanStack Router provider setup
- Import and initialize i18n (`import './lib/i18n'`)
- Wrap app with `<QueryClientProvider>` from TanStack Query
- Wrap app with `<TooltipProvider>` from Radix UI
- Create the router instance and render `<RouterProvider>`

#### `src/App.tsx`
- This file can be removed or repurposed — routing now handled by `src/routes/`
- If kept, reduce to a minimal re-export or delete

#### `src/button.tsx`
- DELETE this file — replaced by `src/components/ui/button.tsx`

#### `index.html`
- Add `data-theme="azure"` attribute to `<html>` element for default theme

---

## Configuration Files to CREATE

#### `.eslintrc.cjs` (or `eslint.config.js`)
- Configure ESLint for TypeScript + React
- Use recommended configs from `@typescript-eslint`

#### `.prettierrc`
- Configure Prettier: semicolons, single quotes, tab width, trailing commas

#### `biome.json`
- Configure Biome linter/formatter as a fast alternative

#### `.github/workflows/ci.yml`
- Create CI pipeline:
  - `cargo check`
  - `cargo clippy -- -D warnings`
  - `cargo test`
  - `pnpm lint`
  - `pnpm build`
  - `pnpm test`
