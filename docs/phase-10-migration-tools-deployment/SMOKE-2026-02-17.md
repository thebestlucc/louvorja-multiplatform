# Phase 10 Smoke Matrix — 2026-02-17

## Scope
Validation run for onboarding, migration, updater, and regression safety per Phase 10 TASKS/SPECS.

## Runtime Evidence

- `pnpm tauri dev` started successfully outside sandbox.
- Vite dev server started and returned `HTTP/1.1 200 OK` on `http://127.0.0.1:5173/`.
- App backend launched (`Running target/debug/louvorja`).

## Static/Build Evidence

- `pnpm exec tsc --noEmit` passed.
- `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- `pnpm exec vite build` passed.
- Locale parity check for EN/PT/ES passed (no missing/extra keys).

## Smoke Matrix Status

1. First run with empty data opens onboarding and completes to dashboard: **BLOCKED**
Reason: Requires GUI interaction and navigation assertions.

2. “Start fresh” path skips migration and writes first-run completion flags: **BLOCKED**
Reason: Requires GUI flow and persisted state verification through app UX path.

3. Legacy database import migrates domains with accurate counts: **BLOCKED**
Reason: Requires interactive import run with real sample legacy DB and UX confirmation.

4. Corrupted/invalid source fails safely with actionable errors and no partial writes: **BLOCKED**
Reason: Requires interactive migration run against intentionally bad source input.

5. Cancel during migration stops work and leaves target DB consistent: **BLOCKED**
Reason: Requires in-flight migration cancellation from UI.

6. Re-run migration after failure works deterministically and records new report: **BLOCKED**
Reason: Requires repeated interactive runs.

7. Updater check with no update is silent and non-blocking: **PARTIAL PASS**
Evidence: Backend command now returns `Ok(None)` on updater unavailability/check errors, preserving non-blocking behavior.

8. Updater check with available update shows release notes and action buttons: **BLOCKED**
Reason: Requires real updater endpoint and an available update artifact.

9. Streaming/projector/return core flows unchanged after Phase 10 additions: **BLOCKED**
Reason: Requires monitor/projector runtime manual validation.

10. Static checks (`tsc`, `cargo check`, `vite build`): **PASS**

## Conclusion

Phase 10 is **NOT ready for closure** yet. Manual GUI-dependent smoke cases remain blocked in this execution environment and must be completed on a machine/session with full interactive UI and monitor control.
